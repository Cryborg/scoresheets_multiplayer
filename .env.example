# Database Configuration
# Production: Use Turso cloud database
TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token

# Development: Use local SQLite
# TURSO_DATABASE_URL=file:./data/scoresheets.db

# Authentication
# CRITICAL: JWT_SECRET must be at least 32 characters and cryptographically secure
# Generate with: openssl rand -hex 32
# NEVER use simple words like 'secret', 'password', 'fallback', etc.
JWT_SECRET=your-cryptographically-secure-64-character-random-string-here-no-words

# Application URL
NEXT_PUBLIC_APP_URL=https://ohsheet.fun

# Email Service (Resend)
# Get your API key from https://resend.com
# Not required for development (uses console logging)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx

# Email Configuration
# From email domain must be verified in Resend
EMAIL_FROM_DOMAIN=ohsheet.fun

# Development Options
NODE_ENV=development